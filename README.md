markdown
# 3D Model Processing

## Required Libraries
```bash
pip install open3d numpy
How to Run
Change the model filename in the code:

python
mesh = o3d.io.read_triangle_mesh("YOUR_MODEL_FILENAME_HERE")
Run in terminal:

bash
python test.py
